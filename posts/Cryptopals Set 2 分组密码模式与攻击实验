<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cryptopals Set 2 - 分组密码模式与攻击实验 | nEwBeE303</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #faf9f8;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
            line-height: 1.7;
            color: #2c3e4e;
            padding: 2rem 1rem;
        }

        .container {
            max-width: 860px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.04);
            padding: 2.5rem 2rem;
        }

        .site-header {
            text-align: center;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid #eaeef2;
        }

        .blog-title {
            font-size: 2.2rem;
            font-weight: 600;
            color: #1a4d6b;
            margin-bottom: 0.3rem;
        }

        .tagline {
            color: #6b8ba5;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .nav {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 0.8rem;
        }

        .nav a {
            text-decoration: none;
            font-size: 0.9rem;
            color: #4a708b;
        }

        .nav a:hover {
            color: #1a4d6b;
            text-decoration: underline;
        }

        .post-header {
            margin-bottom: 2rem;
            text-align: center;
        }

        .post-title {
            font-size: 2rem;
            font-weight: 700;
            color: #1a3a4a;
            margin-bottom: 0.75rem;
        }

        .post-meta {
            font-size: 0.85rem;
            color: #8aa9c2;
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .post-content {
            font-size: 1rem;
            line-height: 1.75;
            color: #2f4a5e;
        }

        .post-content h2 {
            font-size: 1.5rem;
            margin: 1.5rem 0 0.75rem 0;
            padding-bottom: 0.3rem;
            border-bottom: 2px solid #eef2f6;
            color: #1a4d6b;
            scroll-margin-top: 20px;
        }

        .post-content h3 {
            font-size: 1.25rem;
            margin: 1rem 0 0.5rem 0;
            color: #2c6280;
        }

        .post-content p {
            margin-bottom: 1rem;
        }

        .post-content ul, .post-content ol {
            margin: 0.75rem 0 1rem 1.8rem;
        }

        .post-content li {
            margin-bottom: 0.4rem;
        }

        pre {
            background: #1e2a36;
            color: #e2e8f0;
            padding: 1rem 1.2rem;
            border-radius: 12px;
            overflow-x: auto;
            font-family: 'SF Mono', Monaco, monospace;
            font-size: 0.85rem;
            margin: 1.2rem 0;
        }

        code {
            font-family: 'SF Mono', Monaco, monospace;
            background: #f0f4f8;
            padding: 0.2rem 0.4rem;
            border-radius: 6px;
            font-size: 0.85rem;
            color: #c7254e;
        }

        pre code {
            background: transparent;
            color: inherit;
            padding: 0;
        }

        .challenge-card {
            background: #f8fafc;
            border-left: 4px solid #1a4d6b;
            padding: 1rem 1.5rem;
            margin: 1.2rem 0;
            border-radius: 12px;
        }

        .ref-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.6rem;
            margin: 1.5rem 0 1rem;
        }

        .ref-tag {
            background: #eff3f8;
            padding: 0.2rem 0.9rem;
            border-radius: 30px;
            font-size: 0.75rem;
            color: #3a6b8c;
        }

        .license {
            margin: 2rem 0 1rem;
            padding-top: 1rem;
            font-size: 0.8rem;
            color: #8aa2b8;
            text-align: center;
            border-top: 1px solid #eef2f6;
        }

        .share {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin: 1rem 0;
        }

        hr {
            margin: 1.5rem 0;
            border: none;
            border-top: 1px solid #eef2f6;
        }

        .footer {
            text-align: center;
            font-size: 0.8rem;
            color: #8aa2b8;
            margin-top: 2rem;
            padding-top: 1rem;
        }

        .table-of-contents {
            background: #f8fafc;
            border: 1px solid #e6edf4;
            border-radius: 18px;
            padding: 1.2rem 1.5rem;
            margin: 1.5rem 0 1.8rem;
        }

        .toc-title {
            font-size: 1rem;
            font-weight: 600;
            color: #1a4d6b;
            margin-bottom: 0.8rem;
        }

        .toc-list {
            list-style: none;
            margin: 0;
            padding-left: 0;
            display: flex;
            flex-wrap: wrap;
            gap: 0.7rem 1.5rem;
        }

        .toc-list li {
            margin: 0;
        }

        .toc-list a {
            text-decoration: none;
            font-size: 0.9rem;
            color: #3a6b8c;
        }

        .toc-list a:hover {
            color: #1a4d6b;
        }

        .back-link {
            display: inline-block;
            margin-top: 2rem;
            color: #4a708b;
            text-decoration: none;
            font-size: 0.9rem;
        }

        .back-link:hover {
            color: #1a4d6b;
            text-decoration: underline;
        }

        @media (max-width: 620px) {
            .container {
                padding: 1.5rem;
            }
            .post-title {
                font-size: 1.6rem;
            }
            .toc-list {
                flex-direction: column;
                gap: 0.4rem;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <header class="site-header">
        <h1 class="blog-title">nEwBeE303</h1>
        <div class="tagline">记录学习，分享思考</div>
        <div class="nav">
            <a href="/">home</a>
            <a href="/about.html">about</a>
            <a href="/archive.html">archive</a>
        </div>
    </header>

    <main>
        <article>
            <div class="post-header">
                <h1 class="post-title">Cryptopals Set 2 — 分组密码模式与攻击实验</h1>
                <div class="post-meta">
                    <span>📅 发布时间：2026年6月7日</span>
                    <span>✍️ 作者 nEwBeE303</span>
                    <span>📖 12 分钟阅读</span>
                </div>
            </div>

            <div class="post-content">
                <div class="table-of-contents">
                    <div class="toc-title">📑 文章内容</div>
                    <ul class="toc-list">
                        <li><a href="#section-overview">一、实验概述</a></li>
                        <li><a href="#section-challenges">二、挑战实现与原理</a></li>
                        <li><a href="#section-summary">三、实验总结</a></li>
                    </ul>
                </div>

                <h2 id="section-overview">一、实验概述</h2>
                <p>本实验为 Cryptopals 挑战第二组，围绕分组密码的工作模式与攻击技术展开，完整实现了 PKCS#7 填充、CBC 模式、ECB/CBC 检测预言机、逐字节 ECB 解密、CBC 位翻转攻击等核心内容，是现代密码学中分组密码安全实践的基础训练。</p>

                <h2 id="section-challenges">二、挑战实现与原理</h2>

                <div class="challenge-card">
                    <h3>挑战9：实现 PKCS#7 填充</h3>
                    <p>PKCS#7 是一种通用的块填充标准，当明文长度不是块大小的整数倍时，会在末尾补充字节，每个填充字节的值等于填充的字节数。例如，块大小为16时，若明文长度为13，则补充3个值为 <code>0x03</code> 的字节。</p>
                    <pre><code>def pkcs7_pad(data, block_size=16):
    pad_len = block_size - (len(data) % block_size)
    return data + bytes([pad_len] * pad_len)

def pkcs7_unpad(data):
    pad_len = data[-1]
    if pad_len == 0 or any(b != pad_len for b in data[-pad_len:]):
        raise ValueError("Invalid PKCS#7 padding")
    return data[:-pad_len]</code></pre>
                </div>

                <div class="challenge-card">
                    <h3>挑战10：实现 CBC 模式</h3>
                    <p>CBC（密码分组链接）模式是最常用的分组密码模式之一。其核心原理是：前一个密文块与当前明文块异或后，再进行 ECB 加密；解密时则反向操作，前一个密文块与当前 ECB 解密结果异或得到明文。初始向量 IV 用于第一个明文块的异或。</p>
                    <pre><code>from Crypto.Cipher import AES

def aes_ecb_encrypt(plaintext, key):
    cipher = AES.new(key, AES.MODE_ECB)
    return cipher.encrypt(plaintext)

def aes_ecb_decrypt(ciphertext, key):
    cipher = AES.new(key, AES.MODE_ECB)
    return cipher.decrypt(ciphertext)

def cbc_encrypt(plaintext, key, iv):
    plaintext = pkcs7_pad(plaintext)
    ciphertext = b''
    prev_block = iv
    for i in range(0, len(plaintext), 16):
        block = plaintext[i:i+16]
        xor_block = bytes(a ^ b for a, b in zip(block, prev_block))
        encrypted = aes_ecb_encrypt(xor_block, key)
        ciphertext += encrypted
        prev_block = encrypted
    return ciphertext

def cbc_decrypt(ciphertext, key, iv):
    plaintext = b''
    prev_block = iv
    for i in range(0, len(ciphertext), 16):
        block = ciphertext[i:i+16]
        decrypted = aes_ecb_decrypt(block, key)
        plain_block = bytes(a ^ b for a, b in zip(decrypted, prev_block))
        plaintext += plain_block
        prev_block = block
    return pkcs7_unpad(plaintext)</code></pre>
                </div>

                <div class="challenge-card">
                    <h3>挑战11：ECB/CBC 检测预言机</h3>
                    <p>ECB 模式的最大缺陷是相同的明文块会加密为相同的密文块，而 CBC 模式下密文块依赖前一个块，不会出现这种重复。因此，向加密函数发送足够长的重复明文，若密文中出现重复块，则大概率为 ECB 模式。</p>
                    <pre><code>def encryption_oracle(plaintext):
    import random
    key = random.randbytes(16)
    prefix = random.randbytes(random.randint(5, 10))
    suffix = random.randbytes(random.randint(5, 10))
    data = prefix + plaintext + suffix
    if random.choice([True, False]):
        return aes_ecb_encrypt(pkcs7_pad(data), key), "ECB"
    else:
        iv = random.randbytes(16)
        return cbc_encrypt(data, key, iv), "CBC"

def detect_mode(ciphertext, block_size=16):
    blocks = [ciphertext[i:i+block_size] for i in range(0, len(ciphertext), block_size)]
    return "ECB" if len(blocks) != len(set(blocks)) else "CBC"</code></pre>
                </div>

                <div class="challenge-card">
                    <h3>挑战12：字节逐字节 ECB 解密（简单版）</h3>
                    <p>利用固定密钥的 ECB 加密函数，通过构造不同长度的前缀，让未知明文的开头对齐块边界，再通过逐字节爆破，对比密文块恢复出完整明文。</p>
                    <pre><code>unknown_string = b"Um9sbGluJyBpbiBteSA1LjAKV2l0aCBteSByYWctdG9wIGRvd24gc28gbXkg..."
key = random.randbytes(16)

def oracle_ecb(plaintext):
    return aes_ecb_encrypt(pkcs7_pad(plaintext + base64.b64decode(unknown_string)), key)

def discover_block_size():
    l = len(oracle_ecb(b''))
    i = 1
    while True:
        new_len = len(oracle_ecb(b'A'*i))
        if new_len != l:
            return new_len - l
        i += 1

def break_ecb_simple(block_size):
    known = b''
    while True:
        pad_len = (block_size - 1 - len(known)) % block_size
        prefix = b'A' * pad_len
        target_block_idx = len(known) // block_size
        target_cipher = oracle_ecb(prefix)
        target_block = target_cipher[target_block_idx*16 : (target_block_idx+1)*16]
        for byte in range(256):
            test_data = prefix + known + bytes([byte])
            test_cipher = oracle_ecb(test_data)
            if test_cipher[target_block_idx*16 : (target_block_idx+1)*16] == target_block:
                known += bytes([byte])
                break
        else:
            break
    return known</code></pre>
                </div>

                <div class="challenge-card">
                    <h3>挑战13：ECB 复制粘贴攻击</h3>
                    <p>ECB 模式下，每个明文块的加密结果只依赖该块本身，因此可以直接拼接不同密文块，构造出逻辑上无效但解密后为预期明文的密文。这种攻击演示了 ECB 模式不具备语义安全性的根本缺陷。</p>
                </div>

                <div class="challenge-card">
                    <h3>挑战14：字节逐字节 ECB 解密（更难版）</h3>
                    <p>在简单版的基础上，加密函数会在明文前添加固定长度的随机前缀。解题的关键是先通过密文长度变化，确定随机前缀的长度和块边界，再通过偏移对齐的方式，将未知明文推到块边界，使用简单版的方法逐字节爆破。</p>
                </div>

                <div class="challenge-card">
                    <h3>挑战15：PKCS#7 填充验证</h3>
                    <p>实现严格的 PKCS#7 填充验证，确保填充字节的值和数量符合标准。该实现是防御填充预言机攻击的基础，需要检查所有填充字节是否一致，且值不超过块大小。</p>
                </div>

                <div class="challenge-card">
                    <h3>挑战16：CBC 位翻转攻击</h3>
                    <p>利用 CBC 模式的解密特性：<code>P[i] = D(C[i]) XOR C[i-1]</code>。修改 <code>C[i-1]</code> 的某一位，解密后的 <code>P[i]</code> 对应的位就会翻转，从而实现可控的明文篡改。该攻击可用于绕过某些校验逻辑，例如将 <code>admin=false</code> 篡改为 <code>admin=true</code>。</p>
                    <pre><code>def cbc_bitflip_attack(ciphertext, key, iv):
    # 假设目标是修改明文第16个块的内容
    block_num = 1
    pos = 5  # 要翻转的位在第1个密文块中的位置
    ciphertext = bytearray(ciphertext)
    # 翻转前一个密文块的对应位
    ciphertext[(block_num-1)*16 + pos] ^= 1
    return bytes(ciphertext)</code></pre>
                </div>

                <h2 id="section-summary">三、实验总结</h2>
                <p>本次实验完整掌握了分组密码的核心工作模式与攻击技术，理解了 ECB 模式的根本缺陷和 CBC 模式的安全性依赖。通过实现填充、模式、预言机和多种攻击，对现代分组密码的设计与安全边界有了直观认识，为后续学习更复杂的密码协议和攻击打下了坚实基础。</p>

                <div class="ref-tags">
                    <span class="ref-tag">Cryptopals</span>
                    <span class="ref-tag">Set 2</span>
                    <span class="ref-tag">分组密码</span>
                    <span class="ref-tag">CBC模式</span>
                    <span class="ref-tag">ECB攻击</span>
                    <span class="ref-tag">PKCS#7</span>
                    <span class="ref-tag">位翻转攻击</span>
                </div>

                <div class="license">
                    本文由作者按照 CC BY 4.0 进行授权
                </div>
                <div class="share">
                    <span>分享:</span>
                    <span>Twitter</span>
                    <span>GitHub</span>
                    <span>微博</span>
                </div>
                
                <a href="/" class="back-link">← 返回首页</a>
            </div>
        </article>
    </main>

    <hr>
    <footer class="footer">
        © 2026 nEwBeE303 | <a href="https://github.com/nEwbEe303">GitHub</a>
    </footer>
</div>
</body>
</html>
